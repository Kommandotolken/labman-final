<!DOCTYPE html>
<html>
    <head>
       <%include ./partials/head %>
    </head>
    <body>
    <header>
        <% include ./partials/header %>
    </header>
    <p>booking confirm</p>
    <% for (const row of res) { %>
        <tr>
            <td><%= row.namn %></td>
            <td><%= row.id%></td>
            <td><%= row.status %> </td>
            <td><a href="/booking/<%= row.id%>">book</a></td>
        </tr>
    <% }; %>
    <footer>
        <% include ./partials/footer %>
    </footer>
    </body>
</html>